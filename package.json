{
  "name": "kcwiki-quest-data",
  "version": "0.1.0",
  "description": "Kancolle Quest Data",
  "scripts": {
    "generate": "mkdir -p build && quicktype --src types/index.ts --src-lang ts --lang schema --out build/schema.json && ts-node scripts/tweak-schema.ts",
    "build": "ts-node scripts/check-and-build.ts",
    "postinstall": "npm run generate",
    "clean": "rm -rf build/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kcwikizh/kcwiki-quest-data.git"
  },
  "keywords": [
    "Kancolle",
    "Kantai",
    "Collection"
  ],
  "author": "kcwikizh",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/lodash": "^4.14.149",
    "@types/node": "^13.7.7",
    "ajv": "^6.12.0",
    "lodash": "^4.17.15",
    "quicktype": "^15.0.220",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  }
}
